<form method="post" action="{{ path('produto_categoria_delete', {'id': produto_categoria.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_method" value="DELETE">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produto_categoria.id) }}">
	<button class="btn btn-danger btn-sm">Delete</button>
</form>
